# Frontend environment variables
# IMPORTANT:
# - Do NOT include a trailing slash.
# - Do NOT append /api. The client will add routes like /patients, /chat/send.
# - Example correct values:
#     REACT_APP_BACKEND_URL=http://localhost:8000
#     REACT_APP_BACKEND_URL=https://vscode-internal-24827-beta.beta01.cloud.kavia.ai:3001
REACT_APP_BACKEND_URL=
# For local dev when not setting REACT_APP_BACKEND_URL, the app uses "/api".
# Ensure CRA proxy is active via src/setupProxy.js which maps /api -> http://localhost:8000
# Optionally override the proxy target (development only):
# REACT_APP_BACKEND_PROXY_TARGET=http://localhost:8000
